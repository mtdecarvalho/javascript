/**
 * Aritméticos
 * + Adição / concatenação
 * ** Potenciação
 * % Módulo, resto da divisão
 * 
 */

let contador = 1;
console.log(contador++); // Desse método primeiro é impresso o valor e depois ele é incrementado.
console.log(contador);
console.log(++contador); // Dessa forma primeiro é feito o incremento e depois ele é mostrado.

/**
 * A mesma lógica serve tanto para incremento (++) e decremento (--)
 */

// NaN = Not a Number
// parseInt = parse de um inteiro, parseFloat = parse de um número de ponto flutuante

const num1 = 10;
const num2 = "theus";
console.log(num1 * num2)