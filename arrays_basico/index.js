const alunos = ['Luiz', 'Maria', 'João'];

// console.log(alunos instanceof Array);

alunos.unshift('Theus'); // -> adiciona no inicio
alunos.pop(); // -> comportamento padrão, remove do final
alunos.push('Juliana'); // -> adiciona no final
// delete alunos[1]; // apaga um elemento especifico

// console.log(alunos.slice(0, -1));
console.log(alunos);